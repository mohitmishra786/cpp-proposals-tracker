httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
structlog>=24.0.0
tenacity>=8.2.0
tqdm>=4.66.0
pydantic>=2.5.0
aiofiles>=23.2.0
# Pin to <2.19 to avoid storage3's pyiceberg dependency (fails to build on Python 3.14)
supabase==2.18.1
# Primary LLM — Groq via OpenAI-compatible SDK
openai>=1.10.0
groq>=0.9.0
# Primary embeddings — fastembed runs locally, no API key needed (BAAI/bge-small-en-v1.5, 384 dims)
fastembed>=0.3.0
# Alternative: sentence-transformers (same model, pip install sentence-transformers)
# from sentence_transformers import SentenceTransformer
# model = SentenceTransformer("BAAI/bge-small-en-v1.5")
# embeddings = model.encode(texts).tolist()
# Optional: token counting for OpenAI embedding fallback
tiktoken>=0.6.0
# Optional: Anthropic Claude fallback (set LLM_PROVIDER=anthropic)
anthropic>=0.20.0
