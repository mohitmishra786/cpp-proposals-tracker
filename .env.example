# ============================================================
# C++ Proposals Explorer — Environment Variables
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control
# ============================================================

# ---- Supabase -----------------------------------------------
# Found in: Supabase dashboard > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ---- Groq (primary LLM provider) ----------------------------
# Used for Ask AI (llama-3.3-70b-versatile / llama-3.1-8b-instant)
# Found in: https://console.groq.com/keys
GROQ_API_KEY=gsk_...

# LLM backend: "groq" (default) or "anthropic"
LLM_PROVIDER=groq

# ---- Embeddings ---------------------------------------------
# "hf"     — HuggingFace Inference API, BAAI/bge-small-en-v1.5, 384 dims (default)
#            Requires HUGGINGFACE_API_KEY (free tier: https://huggingface.co/settings/tokens)
# "openai" — OpenAI text-embedding-3-small, 1536 dims (requires OPENAI_API_KEY)
EMBEDDING_PROVIDER=hf
HUGGINGFACE_API_KEY=hf_...

# ---- OpenAI (optional fallback) -----------------------------
# Only needed if EMBEDDING_PROVIDER=openai
# Found in: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# ---- Anthropic (optional fallback) -------------------------
# Only needed if LLM_PROVIDER=anthropic
# Found in: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# ---- Upstash Redis ------------------------------------------
# Used for rate limiting the /api/ask endpoint
# Found in: https://console.upstash.com > Redis > REST API
UPSTASH_REDIS_REST_URL=https://your-db.upstash.io
UPSTASH_REDIS_REST_TOKEN=AXxx...

# ---- Crawler (Python only, not used by Next.js) ------------
# These are also added as GitHub Actions secrets for the crawler workflow
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
GROQ_API_KEY=gsk_...
